# Copy to ~/.config/nexus/.env and adapt the paths.

# Acquisition parameter state file, default: ~/nexus-console/acquisition-parameter.state
NEXUS_PARAMETER_FILE=/home/<user>/nexus-console/acquisition-parameter.state

# Directory for exported acquisition data, required unless --export-dir is given
NEXUS_EXPORT_DIR=/home/<user>/nexus-data
